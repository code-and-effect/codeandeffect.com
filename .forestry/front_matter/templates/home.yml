---
label: home
hide_body: false

fields:
- name: title
  type: text
  label: title
  config:
    required: true
- name: description
  type: text
  label: Meta Description
  config:
    required: true
    max: '160'
- name: permalink
  type: text
  label: permalink
  config:
    required: true
- name: redirect_from
  type: list
  config:
    use_select: false
    min:
    max:
  label: redirect_from

- name: services
  type: field_group_list
  label: Services
  description: The services we display on the homepage
  hidden: false
  fields:
  - type: text
    name: service_title
    label: Service Title
    config:
      required: true
  - type: text
    name: service_link
    label: Service Link
    config:
      required: true
  - type: textarea
    name: service_description
    label: Service Description
      config:
      required: true
  config:
    labelField: services

- name: values
  type: field_group_list
  label: Values
  description: The core values we display on the homepage
  hidden: false
  fields:
  - type: text
    name: value_title
    label: Value Title
    config:
      required: true
  - type: textarea
    name: value_description
    label: Value Description
      config:
      required: true
  config:
    labelField: values
